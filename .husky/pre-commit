#!/bin/sh

XDG_DATA_HOME="${XDG_DATA_HOME:-$(pwd)/.socket-data}"
export XDG_DATA_HOME

if [ -z "${SOCKET_CLI_API_TOKEN:-}" ]; then
  echo "Skipping Socket dependency scan: SOCKET_CLI_API_TOKEN is not set."
  exit 0
fi

pnpm security:deps
